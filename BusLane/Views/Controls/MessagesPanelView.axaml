<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:BusLane.ViewModels"
             xmlns:models="using:BusLane.Models"
             xmlns:controls="using:BusLane.Views.Controls"
             xmlns:converters="using:BusLane.Converters"
             x:Class="BusLane.Views.Controls.MessagesPanelView"
             x:DataType="vm:MainWindowViewModel">

    <UserControl.Resources>
        <converters:MessageSelectionConverter x:Key="MessageSelectionConverter"/>
    </UserControl.Resources>

    <Border Classes="card" Padding="0">
        <Grid RowDefinitions="Auto,*">
            <!-- Header Bar with Tabs and Actions -->
            <Border Grid.Row="0" BorderBrush="{DynamicResource BorderDefault}" BorderThickness="0,0,0,1" Padding="16,16,16,12">
                <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="12">
                    <!-- Title and Actions Row -->
                    <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                            <TextBlock Text="Messages" Classes="heading" VerticalAlignment="Center"/>
                            <Border Classes="badge-info"
                                    Padding="8,4"
                                    IsVisible="{Binding CurrentMessageOps.IsSessionScoped}">
                                <TextBlock Text="{Binding CurrentMessageOps.SessionScopeLabel}" FontSize="12"/>
                            </Border>
                            <Button Classes="subtle small"
                                    Command="{Binding CurrentMessageOps.ClearSessionScopeCommand}"
                                    IsVisible="{Binding CurrentMessageOps.IsSessionScoped}"
                                    Padding="8,6">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <LucideIcon Kind="X" Size="12"/>
                                    <TextBlock Text="Clear Session"/>
                                </StackPanel>
                            </Button>
                            <!-- Send Message Button (compact, inline) -->
                            <Button Classes="primary small"
                                    Command="{Binding OpenSendMessagePopupCommand}"
                                    Padding="12,6"
                                    VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <LucideIcon Kind="Plus" Size="12"/>
                                    <TextBlock Text="Send"/>
                                </StackPanel>
                                <Button.IsVisible>
                                    <MultiBinding Converter="{x:Static BoolConverters.And}">
                                        <Binding Path="CurrentNavigation.IsSessionInspectorTabSelected" Converter="{x:Static BoolConverters.Not}"/>
                                        <MultiBinding Converter="{x:Static BoolConverters.Or}">
                                            <Binding Path="CurrentNavigation.SelectedQueue" Converter="{x:Static ObjectConverters.IsNotNull}"/>
                                            <Binding Path="CurrentNavigation.SelectedTopic" Converter="{x:Static ObjectConverters.IsNotNull}"/>
                                        </MultiBinding>
                                    </MultiBinding>
                                </Button.IsVisible>
                            </Button>
                        </StackPanel>
                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    Spacing="6"
                                    IsVisible="{Binding CurrentNavigation.IsSessionInspectorTabSelected, Converter={x:Static BoolConverters.Not}}">
                            <!-- Multi-select toggle button -->
                            <ToggleButton Classes="pill"
                                          IsChecked="{Binding CurrentMessageOps.IsMultiSelectMode, Mode=TwoWay}"
                                          ToolTip.Tip="Toggle multi-select mode (⌘M / Ctrl+M)">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <LucideIcon Kind="Check" Size="12"/>
                                    <TextBlock Text="Select"/>
                                </StackPanel>
                            </ToggleButton>
                            <Button Classes="subtle small"
                                    Content="{Binding CurrentMessageOps.SortButtonText}"
                                    Command="{Binding ToggleSortOrderCommand}"
                                    ToolTip.Tip="Toggle sort order"
                                    Padding="8,8"/>
                            <Button Classes="subtle small"
                                    Command="{Binding LoadMessagesCommand}"
                                    Padding="8,8">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <LucideIcon Kind="RefreshCw" Size="12"/>
                                    <TextBlock Text="Refresh"/>
                                </StackPanel>
                            </Button>
                            <Border Width="1" Background="{DynamicResource BorderDefault}" Margin="4,4"/>
                            <Button Classes="danger-subtle small"
                                    Command="{Binding PurgeMessagesCommand}"
                                    Padding="8,8">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <LucideIcon Kind="Trash2" Size="12"/>
                                    <TextBlock Text="Purge"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Bulk Actions Bar (visible when multi-select mode is active) -->
                    <Border Grid.Row="1"
                            Background="{DynamicResource SurfaceSubtle}"
                            CornerRadius="6"
                            Padding="12,8"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource BorderMuted}">
                        <Border.IsVisible>
                            <MultiBinding Converter="{x:Static BoolConverters.And}">
                                <Binding Path="CurrentMessageOps.IsMultiSelectMode"/>
                                <Binding Path="CurrentNavigation.IsSessionInspectorTabSelected"
                                         Converter="{x:Static BoolConverters.Not}"/>
                            </MultiBinding>
                        </Border.IsVisible>
                        <StackPanel Spacing="8">
                            <!-- Selection info row -->
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Border Classes="badge-info" Padding="8,4">
                                    <TextBlock Text="{Binding CurrentMessageOps.SelectedMessagesCount, StringFormat='{}{0} selected'}"
                                               FontWeight="SemiBold"
                                               FontSize="12"/>
                                </Border>
                                <Button Classes="subtle small"
                                        Content="Select All"
                                        Command="{Binding SelectAllMessagesCommand}"
                                        Padding="8,4"/>
                                <Button Classes="subtle small"
                                        Content="Deselect All"
                                        Command="{Binding DeselectAllMessagesCommand}"
                                        Padding="8,4"/>
                            </StackPanel>
                            <!-- Action buttons row -->
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Button Classes="primary small"
                                        Command="{Binding BulkResendMessagesCommand}"
                                        Padding="12,6"
                                        ToolTip.Tip="Resend selected messages">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <LucideIcon Kind="Send" Size="12"/>
                                        <TextBlock Text="Resend"/>
                                    </StackPanel>
                                </Button>
                                <Button Classes="secondary small"
                                        Command="{Binding ResubmitDeadLetterMessagesAsyncCommand}"
                                        IsVisible="{Binding CurrentNavigation.ShowDeadLetter}"
                                        Padding="12,6"
                                        ToolTip.Tip="Resubmit selected messages from dead letter queue">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <LucideIcon Kind="RefreshCw" Size="12"/>
                                        <TextBlock Text="Resubmit"/>
                                    </StackPanel>
                                </Button>
                                <Button Classes="danger-outline small"
                                        Command="{Binding BulkDeleteMessagesAsyncCommand}"
                                        Padding="12,6"
                                        ToolTip.Tip="Delete selected messages">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <LucideIcon Kind="Trash2" Size="12"/>
                                        <TextBlock Text="Delete"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Search Row -->
                    <Border Grid.Row="2"
                            Background="{DynamicResource SurfaceSubtle}"
                            CornerRadius="6"
                            Padding="8,8"
                            IsVisible="{Binding CurrentNavigation.IsSessionInspectorTabSelected, Converter={x:Static BoolConverters.Not}}">
                        <Grid ColumnDefinitions="Auto,*,Auto">
                            <LucideIcon Grid.Column="0"
                                      Kind="Search"
                                      Size="16"
                                      Foreground="{DynamicResource MutedForeground}"
                                      Margin="4,0,8,0"
                                      VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1"
                                     x:Name="MessageSearchTextBox"
                                     Text="{Binding CurrentMessageOps.MessageSearchText, Mode=TwoWay}"
                                     Watermark="Search by message ID, body, correlation ID, subject..."
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Padding="0,4"
                                     FontSize="13"
                                     VerticalAlignment="Center"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2"
                                    Classes="subtle small"
                                    Command="{Binding ClearMessageSearchCommand}"
                                    IsVisible="{Binding CurrentMessageOps.MessageSearchText, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"
                                    Padding="4"
                                    ToolTip.Tip="Clear search">
                                <LucideIcon Kind="X" Size="12"/>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Tab Content Area -->
            <TabControl Grid.Row="1"
                        SelectedIndex="{Binding CurrentNavigation.SelectedMessageTabIndex}">

                <TabItem Header="Active Messages">
                    <TabItem.Content>
                        <Grid RowDefinitions="*,Auto">
                            <ListBox Grid.Row="0"
                                     ItemsSource="{Binding CurrentMessageOps.FilteredMessages}"
                                     SelectedItem="{Binding CurrentMessageOps.SelectedMessage}"
                                     Background="Transparent"
                                     Padding="8"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.Styles>
                            <Style Selector="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.Styles>
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate x:DataType="models:MessageInfo">
                                    <Grid ColumnDefinitions="Auto,*">
                                        <!-- Checkbox for multi-select mode -->
                                        <CheckBox Grid.Column="0"
                                                  IsVisible="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).CurrentMessageOps.IsMultiSelectMode}"
                                                  Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).ToggleMessageSelectionCommand}"
                                                  CommandParameter="{Binding}"
                                                  Margin="0,0,8,0"
                                                  VerticalAlignment="Center">
                                            <CheckBox.IsChecked>
                                                <MultiBinding Converter="{StaticResource MessageSelectionConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Path="$parent[Window].((vm:MainWindowViewModel)DataContext).CurrentMessageOps.SelectedMessages"/>
                                                    <Binding Path="$parent[Window].((vm:MainWindowViewModel)DataContext).CurrentMessageOps.SelectionVersion"/>
                                                </MultiBinding>
                                            </CheckBox.IsChecked>
                                        </CheckBox>
                                    <Button Grid.Column="1"
                                            Classes="subtle"
                                            Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).SelectMessageCommand}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            Padding="12,12"
                                            Margin="0,2">
                                        <Button.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Resend"
                                                          Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).ResendMessageCommand}"
                                                          CommandParameter="{Binding}">
                                                    <MenuItem.Icon>
                                                        <LucideIcon Kind="Send" Size="16"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem Header="Clone"
                                                          Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).CloneMessageCommand}"
                                                          CommandParameter="{Binding}">
                                                    <MenuItem.Icon>
                                                        <LucideIcon Kind="Pencil" Size="16"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                            </ContextMenu>
                                        </Button.ContextMenu>
                                        <StackPanel>
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding MessageId}"
                                                           FontWeight="SemiBold"
                                                           FontSize="12"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding EnqueuedTime, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"
                                                           Classes="caption"
                                                           FontSize="10"
                                                           Foreground="{DynamicResource MutedForeground}"/>
                                            </Grid>
                                            <TextBlock Text="{Binding BodyPreview}"
                                                       Classes="caption"
                                                       FontSize="11"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxLines="2"
                                                       Margin="0,6,0,0"
                                                       Foreground="{DynamicResource SubtleForeground}"
                                                       IsVisible="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).EnableMessagePreview}"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0" Spacing="6">
                                                <Border Classes="badge-muted" Padding="6,3">
                                                    <TextBlock Text="{Binding SequenceNumber, StringFormat='Seq: {0}'}"
                                                               Foreground="{DynamicResource SubtleForeground}" FontSize="10"/>
                                                </Border>
                                                <Border Classes="badge-muted" Padding="6,3" IsVisible="{Binding DeliveryCount}">
                                                    <TextBlock Text="{Binding DeliveryCount, StringFormat='Delivery: {0}'}"
                                                               Foreground="{DynamicResource SubtleForeground}" FontSize="10"/>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                            <controls:MessagePaginationFooterView Grid.Row="1"
                                                                  Margin="8,8,8,0"
                                                                  DataContext="{Binding CurrentMessageOps}"/>

                            <!-- Beautiful centered loading overlay -->
                            <Border Grid.RowSpan="2" IsVisible="{Binding CurrentMessageOps.IsLoadingMessages}"
                                    Background="{DynamicResource LoadingOverlay}"
                                    IsHitTestVisible="True">
                                <Border Background="{DynamicResource CardBackground}"
                                        Padding="32,24"
                                        CornerRadius="12"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource BorderDefault}"
                                        BoxShadow="0 8 24 0 #32000000"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        MinWidth="280">
                                    <StackPanel Spacing="16" HorizontalAlignment="Center">
                                        <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                                                     Foreground="{DynamicResource AccentBrand}"
                                                     Background="{DynamicResource BorderDefault}"/>
                                        <StackPanel Spacing="8" HorizontalAlignment="Center">
                                            <TextBlock Text="Loading Messages"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource AppForeground}"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="Please wait while we fetch the messages..."
                                                       FontSize="13"
                                                       Foreground="{DynamicResource SubtleForeground}"
                                                       HorizontalAlignment="Center"
                                                       TextAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Border>
                        </Grid>
                    </TabItem.Content>
                </TabItem>

                <TabItem Header="Dead Letter">
                    <!-- Loading indicator for dead letter messages -->
                    <TabItem.Content>
                        <Grid RowDefinitions="*,Auto">
                            <ListBox Grid.Row="0"
                                     ItemsSource="{Binding CurrentMessageOps.FilteredMessages}"
                             SelectedItem="{Binding CurrentMessageOps.SelectedMessage}"
                             Background="Transparent"
                             Padding="8"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.Styles>
                            <Style Selector="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.Styles>
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                                <DataTemplate x:DataType="models:MessageInfo">
                                    <Grid ColumnDefinitions="Auto,*">
                                        <!-- Checkbox for multi-select mode -->
                                        <CheckBox Grid.Column="0"
                                                  IsVisible="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).CurrentMessageOps.IsMultiSelectMode}"
                                                  Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).ToggleMessageSelectionCommand}"
                                                  CommandParameter="{Binding}"
                                                  Margin="0,0,8,0"
                                                  VerticalAlignment="Center">
                                            <CheckBox.IsChecked>
                                                <MultiBinding Converter="{StaticResource MessageSelectionConverter}">
                                                    <Binding Path="."/>
                                                    <Binding Path="$parent[Window].((vm:MainWindowViewModel)DataContext).CurrentMessageOps.SelectedMessages"/>
                                                    <Binding Path="$parent[Window].((vm:MainWindowViewModel)DataContext).CurrentMessageOps.SelectionVersion"/>
                                                </MultiBinding>
                                            </CheckBox.IsChecked>
                                        </CheckBox>
                                    <Button Grid.Column="1"
                                            Classes="subtle"
                                            Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).SelectMessageCommand}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            Padding="12,12"
                                            Margin="0,2">
                                        <Button.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Resend"
                                                          Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).ResendMessageCommand}"
                                                          CommandParameter="{Binding}">
                                                    <MenuItem.Icon>
                                                        <LucideIcon Kind="Send" Size="16"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem Header="Clone"
                                                          Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).CloneMessageCommand}"
                                                          CommandParameter="{Binding}">
                                                    <MenuItem.Icon>
                                                        <LucideIcon Kind="Pencil" Size="16"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                            </ContextMenu>
                                        </Button.ContextMenu>
                                        <StackPanel>
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding MessageId}"
                                                           FontWeight="SemiBold"
                                                           FontSize="12"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="{Binding EnqueuedTime, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"
                                                           Classes="caption"
                                                           FontSize="10"
                                                           Foreground="{DynamicResource MutedForeground}"/>
                                            </Grid>
                                            <!-- Dead Letter Reason -->
                                            <Border Background="{DynamicResource SurfaceDanger}" Padding="8,4" Margin="0,6,0,0" CornerRadius="4"
                                                    IsVisible="{Binding DeadLetterReason, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                                                <TextBlock Text="{Binding DeadLetterReason}"
                                                           Foreground="{DynamicResource TextDanger}"
                                                           FontSize="11"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </Border>
                                            <TextBlock Text="{Binding BodyPreview}"
                                                       Classes="caption"
                                                       FontSize="11"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxLines="2"
                                                       Margin="0,6,0,0"
                                                       Foreground="{DynamicResource SubtleForeground}"
                                                       IsVisible="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).EnableMessagePreview}"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0" Spacing="6">
                                                <Border Classes="badge-muted" Padding="6,3">
                                                    <TextBlock Text="{Binding SequenceNumber, StringFormat='Seq: {0}'}"
                                                               Foreground="{DynamicResource SubtleForeground}" FontSize="10"/>
                                                </Border>
                                                <Border Classes="badge-muted" Padding="6,3" IsVisible="{Binding DeliveryCount}">
                                                    <TextBlock Text="{Binding DeliveryCount, StringFormat='Delivery: {0}'}"
                                                               Foreground="{DynamicResource SubtleForeground}" FontSize="10"/>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                    </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                            <controls:MessagePaginationFooterView Grid.Row="1"
                                                                  Margin="8,8,8,0"
                                                                  DataContext="{Binding CurrentMessageOps}"/>

                            <!-- Beautiful centered loading overlay -->
                            <Border Grid.RowSpan="2" IsVisible="{Binding CurrentMessageOps.IsLoadingMessages}"
                                    Background="{DynamicResource LoadingOverlay}"
                                    IsHitTestVisible="True">
                                <Border Background="{DynamicResource CardBackground}"
                                        Padding="32,24"
                                        CornerRadius="12"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource BorderDefault}"
                                        BoxShadow="0 8 24 0 #32000000"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        MinWidth="280">
                                    <StackPanel Spacing="16" HorizontalAlignment="Center">
                                        <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                                                     Foreground="{DynamicResource AccentBrand}"
                                                     Background="{DynamicResource BorderDefault}"/>
                                        <StackPanel Spacing="8" HorizontalAlignment="Center">
                                            <TextBlock Text="Loading Dead Letter Messages"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource AppForeground}"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="Please wait while we fetch the dead letter messages..."
                                                       FontSize="13"
                                                       Foreground="{DynamicResource SubtleForeground}"
                                                       HorizontalAlignment="Center"
                                                       TextAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Border>
                        </Grid>
                    </TabItem.Content>
                </TabItem>

                <TabItem Header="Sessions"
                         IsVisible="{Binding CurrentNavigation.CanShowSessionInspector}">
                    <TabItem.Content>
                        <Grid RowDefinitions="Auto,*,Auto">
                            <Border Grid.Row="0"
                                    Background="{DynamicResource SurfaceSubtle}"
                                    CornerRadius="6"
                                    Padding="12,10"
                                    Margin="8,8,8,0">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <TextBlock Text="Discovered sessions"
                                                   FontWeight="SemiBold"
                                                   FontSize="13"/>
                                        <TextBlock Text="Session IDs are discovered from active and dead-letter message peeks."
                                                   Classes="caption"
                                                   Foreground="{DynamicResource SubtleForeground}"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                            Classes="subtle small"
                                            Command="{Binding CurrentSessionInspector.LoadSessionsCommand}"
                                            Padding="10,8">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <LucideIcon Kind="RefreshCw" Size="12"/>
                                            <TextBlock Text="Refresh Sessions"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>

                            <ListBox Grid.Row="1"
                                     ItemsSource="{Binding CurrentSessionInspector.Sessions}"
                                     SelectedItem="{Binding CurrentSessionInspector.SelectedSession}"
                                     Background="Transparent"
                                     Padding="8"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.Styles>
                                    <Style Selector="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListBox.Styles>
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate x:DataType="models:SessionInspectorItem">
                                        <Border Classes="card" Padding="12" Margin="0,0,0,8">
                                            <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="8">
                                                <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
                                                    <StackPanel Grid.Column="0" Spacing="4">
                                                        <TextBlock Text="{Binding SessionId}"
                                                                   FontWeight="SemiBold"
                                                                   FontSize="13"/>
                                                        <TextBlock Text="{Binding State, StringFormat='State: {0}'}"
                                                                   Classes="caption"
                                                                   Foreground="{DynamicResource SubtleForeground}"
                                                                   IsVisible="{Binding State, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                                    </StackPanel>
                                                    <Button Grid.Column="1"
                                                            Classes="primary small"
                                                            Command="{Binding $parent[Window].((vm:MainWindowViewModel)DataContext).CurrentSessionInspector.OpenSessionMessagesCommand}"
                                                            CommandParameter="{Binding}"
                                                            Padding="10,8">
                                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                                            <LucideIcon Kind="ArrowRight" Size="12"/>
                                                            <TextBlock Text="Open Messages"/>
                                                        </StackPanel>
                                                    </Button>
                                                </Grid>

                                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="6">
                                                    <Border Classes="badge-muted" Padding="6,3">
                                                        <TextBlock Text="{Binding ActiveMessageCount, StringFormat='Active: {0}'}"
                                                                   FontSize="10"
                                                                   Foreground="{DynamicResource SubtleForeground}"/>
                                                    </Border>
                                                    <Border Classes="badge-muted" Padding="6,3">
                                                        <TextBlock Text="{Binding DeadLetterMessageCount, StringFormat='DLQ: {0}'}"
                                                                   FontSize="10"
                                                                   Foreground="{DynamicResource SubtleForeground}"/>
                                                    </Border>
                                                    <Border Classes="badge-info"
                                                            Padding="6,3"
                                                            IsVisible="{Binding HasDeadLetter}">
                                                        <TextBlock Text="Dead Letter Present" FontSize="10"/>
                                                    </Border>
                                                </StackPanel>

                                                <StackPanel Grid.Row="2" Spacing="2">
                                                    <TextBlock Text="{Binding LastActivityAt, StringFormat='Last activity: {0:yyyy-MM-dd HH:mm:ss}'}"
                                                               Classes="caption"
                                                               Foreground="{DynamicResource SubtleForeground}"/>
                                                    <TextBlock Text="{Binding LockedUntil, StringFormat='Locked until: {0:yyyy-MM-dd HH:mm:ss}'}"
                                                               Classes="caption"
                                                               Foreground="{DynamicResource SubtleForeground}"
                                                               IsVisible="{Binding LockedUntil, Converter={x:Static ObjectConverters.IsNotNull}}"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Border Grid.RowSpan="3"
                                    IsVisible="{Binding CurrentSessionInspector.IsLoadingSessions}"
                                    Background="{DynamicResource LoadingOverlay}"
                                    IsHitTestVisible="True">
                                <Border Background="{DynamicResource CardBackground}"
                                        Padding="32,24"
                                        CornerRadius="12"
                                        BorderThickness="1"
                                        BorderBrush="{DynamicResource BorderDefault}"
                                        BoxShadow="0 8 24 0 #32000000"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        MinWidth="280">
                                    <StackPanel Spacing="16" HorizontalAlignment="Center">
                                        <ProgressBar IsIndeterminate="True"
                                                     Width="200"
                                                     Height="4"
                                                     Foreground="{DynamicResource AccentBrand}"
                                                     Background="{DynamicResource BorderDefault}"/>
                                        <StackPanel Spacing="8" HorizontalAlignment="Center">
                                            <TextBlock Text="Loading Sessions"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource AppForeground}"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="Please wait while we inspect the available sessions..."
                                                       FontSize="13"
                                                       Foreground="{DynamicResource SubtleForeground}"
                                                       HorizontalAlignment="Center"
                                                       TextAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Border>
                        </Grid>
                    </TabItem.Content>
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</UserControl>
